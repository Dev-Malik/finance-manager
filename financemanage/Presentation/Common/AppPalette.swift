import SwiftUI

enum AppPalette {
    static let screenBackground = Color(red: 0.10, green: 0.10, blue: 0.11)
    static let cardBackground = Color(red: 0.08, green: 0.09, blue: 0.11)
    static let cardBorder = Color.white.opacity(0.06)
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.65)
    static let inputBackground = Color.white.opacity(0.03)
}
